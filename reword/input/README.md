# reword/input — drop question PDFs here

Put one or more question PDFs in this folder, then tell Claude **"process reword"**.

- Each PDF's filename becomes the JSON name (and the default title/remark).
- PDFs are git-ignored (never committed).
- Output lands in `../output/<name>.json` — upload-ready.
