# json-output

Verified, upload-ready question JSON lands here — one file per sheet, named after the
source PDF. Each file is only written **after** the 3× check (question renders, answer
re-solved, full re-read vs the PDF). Review here, then it uploads to live.
